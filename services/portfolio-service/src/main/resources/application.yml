spring:
  application:
    name: portfolio-service

  datasource:
    url: jdbc:postgresql://localhost:5432/marketpulse_portfolio
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

user-service:
  url: http://localhost:8082